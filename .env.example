# =============================================================================
# .env.example — 실제 값을 채워 .env 로 복사해서 사용 (.env 는 gitignore 대상)
# =============================================================================

# 배포용
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=sixfin

# 로컬
DB_HOST=
DB_USERNAME=
DB_PASSWORD=
